import { CarparkItem } from "./CarparkItem"



export function CarparkList({ fullCarparkList, filters }) {

    // todo: iterate each carparkItem in the list and render it only if it passes the filter
    // return (<ol className="list"> 
    //     {carparkList.length === 0 && "No carpark in the list"}
    //     {
        
    //     {carparkList.map(item => {
    //         return <CarparkItem
    //             id={item.id}
    //             address={item.address}
    //             availLots={item.availLots}
    //         />
    //     })}
            
    //     }
    // </ol>)

}
